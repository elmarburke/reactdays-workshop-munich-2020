query Messages {
  messages {
    id
    text
    author
  }
}

mutation SendMessage($text: String!, $author: String!) {
  sendMessage(input: { text: $text, author: $author }) {
    id
    text
    author
  }
}
